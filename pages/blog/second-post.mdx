---
title: "Second post"
date: "2025-09-02"
tags: ["notes", "update"]
summary: "A second post to validate tags and RSS"
readingTime: 1
masthead: true
---

import PostLayoutWrapper from "../../components/PostLayoutWrapper";

export default ({ children, ...props }) => (
  <PostLayoutWrapper frontmatter={props.frontmatter}>{children}</PostLayoutWrapper>
);

# Second post

This validates frontmatter, tags, and RSS generation.

```tsx
export default function Example() {
  return <div>example</div>;
}
```

## Why this matters

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Vivamus et lectus vitae orci euismod semper. Phasellus tempus, mauris at convallis mollis, odio lacus mollis mauris, ut maximus justo massa at risus.

### Approach

We break the problem down:

1. Define constraints and success metrics.
2. Build small, testable pieces.
3. Iterate with measurement.

### Notes

- Keep code paths short and explicit.
- Prefer pure functions for core logic.
- Document assumptions explicitly.

## Implementation details

```ts
export function clamp(v: number, min: number, max: number): number {
  if (Number.isNaN(v)) return min;
  return Math.max(min, Math.min(max, v));
}
```

Donec nec feugiat nisl. Integer dictum sed justo at dapibus. Praesent placerat, libero ut cursus vehicula, est sem faucibus dui, nec ultrices arcu dui at mauris.
